//synchronous
function displayNumbers(times) {
    for (let index = 0; index < times; index++) {
        
        console.log(index);
    }
}

function clone(object) {
    Object.create()
    document.querySelectorAll('div.hero-section input[checked]')
}
// console.log('Starting')
// displayNumbers(5)
// console.log('Finished')

// asynchronous

// console.log('Starting')
// setTimeout(() => displayNumbers(5), 0)
// console.log('Finished')
